// You have to write functions using the function keyword 
// as using arrow functions resolves in your function not being callable!

export function square(number: i32): i32 {
  return number * number;
};
